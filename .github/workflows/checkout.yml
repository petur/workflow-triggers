name: Checkout shallow

on: push

jobs:
  shallow:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        ref: ${{ github.ref }} refs/heads/main
    - run: git br -a
    - run: git log --oneline
