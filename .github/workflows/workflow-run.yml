on:
  workflow_run:
    workflows:
    - Workflow name
    types:
    - completed
    branches:
    - workflow

jobs:
  stuff:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
    - run: echo Hello workflow

