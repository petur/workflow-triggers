name: Inputs

on:
  push:
  workflow_dispatch:
    inputs:
      foo:
        description: A footastic input
        type: string

jobs:
  print:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: 'echo "foo: ${{ inputs.foo }}"'
    - name: 'Step name'
      uses: ./.github/actions/test
      with:
        an_input: 'A provided values'
